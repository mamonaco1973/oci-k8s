# OCI replaces boto3/botocore/s3transfer/jmespath from the AWS build. The oci
# package carries the NoSQL client and the workload identity signer together,
# so nothing else is needed for storage or auth.
oci>=2.130.0
flask>=2.2.5
gunicorn>=22.0.0
click==8.0.1
itsdangerous==2.0.1
jinja2>=3.1.5
MarkupSafe>=2.1.1
python-dateutil==2.8.1
six==1.16.0
urllib3>=1.26.19
Werkzeug>=3.0.6
